# Fixtures

All fixtures in this directory are synthetic examples maintained for deterministic
tests and documentation. They do not contain user conversations, Personal Atlas
data, provider configuration, or private capsule contents.

Fixed identifiers and timestamps are test data, not production records. The
`origin-conversation` fixture is an authored reference example derived from the
public concepts in `docs/DESIGN.md`; it is not a historical transcript.
